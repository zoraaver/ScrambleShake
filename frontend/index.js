// grabbing user inputs and update the db
let from = document.querySelector('sign-in');
let gameBegins = form.querySelector('submit-user');

gameBegins.addEventListener('click',createUsers);

const createUsers = e => {
    e.preventDefault(); 
    
};